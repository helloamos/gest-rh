<%= form_with(model: sanction, local: false) do |form| %>
  <div class="modal-body">

  <% if sanction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sanction.errors.count, "error") %> prohibited this sanction from being saved:</h2>

      <ul>
      <% sanction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :matricule_employe %>
    <%= form.text_field :matricule_employe, id: :sanction_matricule_employe, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :date_debut %>
    <%= form.date_field :date_debut, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :date_fin %>
    <%= form.date_field :date_fin, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :type_sanction %>
      <%= form.collection_select(:type_sanction_id, @type_sanctions,  :id, :nom , {prompt: "Type de congé"}, class: "form-control", required: true) %>

  </div>

  <div class="form-group">
    <%= form.label :motif %>
    <%= form.text_area :motif,  class: "form-control" %>
  </div>

  </div><!-- modal body -->
  <div class="modal-footer">
      <%= link_to "Annuler", "#", class: "btn", data: {dismiss: "modal"} %>
      <%= form.submit "Enregistrer", class: "btn btn-primary",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
  </div>
<% end %>
