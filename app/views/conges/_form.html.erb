<%= form_with(model: conge, local: false) do |form| %>
  <div class="modal-body">
  <% if conge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conge.errors.count, "error") %> prohibited this conge from being saved:</h2>

      <ul>
      <% conge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="form-group">
    <%= form.label :matricule_employe %>
    <%= form.text_field :matricule_employe, id: :conge_matricule_employe, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :type_conge_id %>
    <%= form.collection_select(:type_conge_id, @type_conges,  :id, :name , {prompt: "Type de congé"}, class: "form-control", required: true) %>

  </div>

  <div class="form-group">
    <%= form.label :date_debut %>
    <%= form.date_field :date_debut, id: :conge_date_debut, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :date_fin %>
    <%= form.date_field :date_fin, id: :conge_date_fin, class: "form-control", required: true %>
  </div>

  <div class="form-group">
    <%= form.label :motif %>
    <%= form.text_area :motif, id: :conge_cause, class: "form-control", required: true %>
  </div>

 

  </div><!-- modal body -->
  <div class="modal-footer">
      <%= link_to "Annuler", "#", class: "btn", data: {dismiss: "modal"} %>
      <%= form.submit "Enregistrer", class: "btn btn-primary",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
  </div>
<% end %>
