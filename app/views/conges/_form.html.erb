<%= form_with(model: conge, local: true) do |form| %>
  <div class="modal-body">
  <% if conge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conge.errors.count, "error") %> prohibited this conge from being saved:</h2>

      <ul>
      <% conge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :type_conge_id %>
    <%= form.text_field :type_conge_id, id: :conge_type_conge_id , class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :date_debut %>
    <%= form.date_field :date_debut, id: :conge_date_debut, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :cause %>
    <%= form.text_area :cause, id: :conge_cause, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :matricule_employe_id %>
    <%= form.text_field :matricule_employe_id, id: :conge_matricule_employe_id, class: "form-control" %>
  </div>

  </div><!-- modal body -->
  <div class="modal-footer">
      <%= link_to "Annuler", "#", class: "btn", data: {dismiss: "modal"} %>
      <%= form.submit "Enregistrer", class: "btn btn-primary",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
  </div>
<% end %>
